checks:
  required_workflows:
    codeql.yaml: False
    audit.yaml: False
    dependabot-auto-merge.yaml: False
    clean.yaml: False
  versions:
    audit: False

publish:
  pypi:
    packages: []
  docker:
    images:
      - name: camptocamp/qgis-server
